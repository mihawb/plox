fun addPair(a, b) {
/*
 * C-style comment that will be discarded by lexer
 */
  return a + b;
}

fun identity(a) {
  return a;
} // inline comment that will be discarded by lexer

print identity(addPair)/* this one too! */(1, 2);

var none = nil;
